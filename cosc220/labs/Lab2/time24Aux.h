#ifndef TIME24AUX_H
#define TIME24AUX_H
#include "time24.h"

  bool isLater(const time24& t1, const time24& t2);
      //takes 2 time24 objects and determine wether the first
      // is later than the second

#endif
